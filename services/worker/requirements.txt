httpx>=0.27
cryptography>=43.0     # AES-256-GCM for the credential vault
fastapi>=0.115
uvicorn[standard]>=0.30
psycopg[binary,pool]>=3.2
pydantic>=2.9
dnspython>=2.6         # DNS doctor: SPF/DKIM/DMARC/MX + blocklist lookups
pytest>=8.3
pytest-asyncio>=0.24

# Removed rather than left aspirational: pynacl (superseded by cryptography),
# imap-tools (the IMAP client uses the stdlib), celery, tenacity and python-dotenv
# (not yet imported anywhere). Add them back in the commit that first uses them -
# an unused dependency is image weight and a false impression of what runs.
